var React = require('react');

var Examples = React.createClass({
    render: function () {
        return (
            <h2>Example Component</h2>
        )
    }
}); // end of example

module.exports = Examples;